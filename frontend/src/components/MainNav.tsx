import { Button } from './ui/button'

const MainNav = () => {
  return (
    <div>
      <Button
        className="font-bold bg-orange-500"
        onClick={async () => {}}
      >
        Log In
      </Button>

    </div>
  )
}

export default MainNav